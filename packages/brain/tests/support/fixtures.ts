// `ActivityBuilder` is now a public part of the engine (SPEC.md's own
// worked examples use it) — this file just re-exports it so existing test
// imports keep working unchanged.
export { ActivityBuilder, activity } from "../../src/engine/activity-builder"
